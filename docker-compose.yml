services:
  smp-server:
    build: .
    image: ghcr.io/sdjvdf-distributed-computing/echo-server:local
    env_file: .env
    ports:
      - "${SMP_PORT:-8443}:${SMP_PORT:-8443}"
    volumes:
      - ./certs:/app/certs:ro
    restart: unless-stopped
